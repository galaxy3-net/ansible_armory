---
# tasks file for ansible

- name: Show Hostname
  debug:
    msg: "Hostname is {{ ansible_hostname }} - {{ ansible_os_family }}"

- name: Print all available facts
  ansible.builtin.debug:
    var: ansible_facts

- name: Clone Armory
  git:
    repo: https://github.com/galaxy3-net/armory.git
    dest: /home/vagrant/armory2
    single_branch: clone
    version: armory

